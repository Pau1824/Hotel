{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build -- --configuration=production"
  },
  "deploy": {
    "startCommand": "npx serve -s dist/sistema/browser"
  }
}
